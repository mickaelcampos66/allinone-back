{% extends 'base.html.twig' %}

{% block title %}AllinOne - Listing des Séries{% endblock %}

{% block body %}

<h2>Liste des Séries</h2>
<div class="d-flex justify-content-end">
  <a class="btn btn-primary" href="{{ path('backoffice_shows_add') }}">Ajouter</a>
</div>
  <table class="table table-hover mt-4">
  <thead>
      <tr>
          <th scope="col">#</th>
          <th scope="col">Titre</th>
          <th scope="col">Date de sortie</th>
          <th scope="col">Sommaire</th>
          <th scope="col"></th>
      </tr>
  </thead>
<tbody>
  {% for show in shows|reverse %}
      <tr>
      <th scope="row">{{ show.id }}</th>
      <td>{{show.title }}</td>
      <td>{{show.releaseDate }}</td>
      <td>{{ show.summary}}</td>
      <td class="text-end">
          <a href="{{ path('backoffice_shows_update', {"id": show.id}) }}" class="btn btn-sm btn-warning">
              <i class="fa fa-pencil-square-o" aria-hidden="true"></i>Modifier
          </a>
          <a href="{{ path('backoffice_shows_delete', {"id": show.id}) }}" class="btn btn-sm btn-danger mediaDelete">
            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>Supprimer
          </a>
      </td>
  </tr>
  {% endfor %}
  </tbody>
</table>        

{% endblock %}