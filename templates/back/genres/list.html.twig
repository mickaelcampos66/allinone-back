{% extends 'base.html.twig' %}

{% block title %}AllinOne - Listing des genres{% endblock %}

{% block body %}

<h2>Liste des genres</h2>
<div class="d-flex justify-content-end">
  <a class="btn btn-primary" href="{{ path('backoffice_genres_add') }}">Ajouter</a>
</div>
  <table class="table table-hover mt-4">
  <thead>
      <tr>
          <th scope="col">#</th>
          <th scope="col">Nom du genre</th>
          <th scope="col"></th>
      </tr>
  </thead>
<tbody>
  {% for genre in genres|reverse %}
      <tr>
      <th scope="row">{{ genre.id }}</th>
      <td>{{genre.name }}</td>
      <td class="text-end">
          <a href="{{ path('backoffice_genres_update', {"id": genre.id}) }}" class="btn btn-sm btn-warning">
              <i class="fa fa-pencil-square-o" aria-hidden="true"></i>Modifier
          </a>
          <a href="{{ path('backoffice_genres_delete', {"id": genre.id}) }}" class="btn btn-sm btn-danger mediaDelete">
            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>Supprimer
          </a>
      </td>
      </td>
  </tr>
  {% endfor %}
  </tbody>
</table>        

{% endblock %}